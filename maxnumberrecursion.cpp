#include<bits/stdc++.h>

using namespace std;

int maximum(long long arr[],int n)
{
     if(n>0)
     {
          if(arr[n-1]>arr[n])
          {
               return maximum
          }
     }
}

int main()
{
     int n;
     cin>>n;

     long long arr[n];
     for(int i=0;i<n;i++)
          cin>>arr[i];

     maximum(arr,n);

     return 0;
}